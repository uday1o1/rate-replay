# Contributing

Read [BUILD_PLAN.md](BUILD_PLAN.md) and the repository instructions before changing behavior.
Every supported calculation requires source-linked evidence and a regression test.
Every full sentence in a long Markdown file should occupy its own physical line.

Run `make check` before committing.
Never commit private utility data, generated uploads, credentials, model artifacts, local databases, or benchmark caches.
