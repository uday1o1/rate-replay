[extend]
useDefault = true

[[allowlists]]
description = "Tariff explanation identifiers and bounded object reads are not credentials"
targetRules = ["generic-api-key"]
regexTarget = "match"
regexes = [
  '''explanation_key":"tariff\.[a-z0-9_.]+"''',
  '''^key,\s+maximum_bytes=64\s$''',
]
