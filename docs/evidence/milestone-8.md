# Milestone 8 evidence

State: `IMPLEMENTED_PENDING_GATE`.

Every Milestone 8 software deliverable and automated qualification is implemented and passing.
Milestone acceptance is deferred because the required five genuine first-time participant sessions have not been recorded.
Synthetic persona sessions are development-only protocol quality assurance, contribute zero participants, and are not human evidence.

## Frozen evaluation

`benchmarks/manifests/m8-evaluation-v1.json` was frozen before execution with content hash `06186b97f28994d05144175d9b7b574ac441bbc2949524c2898d2c27984d0cf9`.
It locks the public fixtures, tariff sources, independent derivation runner, workloads, thresholds, repetitions, negative cases, local hardware description, and human-study status.
The deterministic `evidence/evaluation/m8-summary.json` indexes every qualified source by both file hash and artifact self-hash.
The combined `evidence/evaluation/m8-performance.json`, raw measurement table at `docs/results/m8-performance.csv`, and accessible chart at `docs/results/m8-performance.svg` are regenerated by `make finalize-m8-evaluation` and checked without mutation by `make m8-evaluation-check`.

## Correctness results

The independent billing runner imports no production package and derived five source-linked cases covering all five admitted tariff plans.
All five independent derivations matched the production golden results.
The production corpus retains six complete-bill and boundary fixtures linked to locked public source sheets.

The parser qualification admitted the locked 362-reading ESPI fixture and the 5,664-reading provider-produced CSV fixture.
It rejected external entities, XML entity expansion, non-integral watt-hours, and an unknown public adapter value with the intended public error codes.

The comparison qualification included all five admitted candidates.
Every candidate was eligible and complete across the five required component classes.
The frozen simulated profile ranked E-TOU-D first, while an unknown eligibility fact and a removed active-component declaration both suppressed winners and savings.

The optimizer oracle exhaustively enumerated 25 small integer cases across all five admitted tariffs.
All 125 lowering-versus-fresh-replay checks matched exactly, every selected schedule belonged to the complete optimum set, and all four objective stages were proved optimal.
The seeded one-watt-hour corruption failed with `VERIFIER_ENERGY_CONSERVATION_FAILED` while its controls passed.

## Performance and recovery results

Every measured repetition is retained in the underlying artifacts and in the generated CSV view.
Cold and warm series remain separate.
The original high-variance results remain unchanged, and the one-year warm import plus cold and warm replay follow-ups reduced coefficient of variation to at most 0.050883 while preserving deterministic result hashes.

| Measurement                                      | Repetitions |           p95 | Frozen threshold | Result |
| ------------------------------------------------ | ----------: | ------------: | ---------------: | ------ |
| One-year import, warm variance follow-up         |          10 |    427.883 ms |        15,000 ms | Pass   |
| Five-load optimization, warm                     |          10 |  2,568.952 ms |        30,000 ms | Pass   |
| Warm cached comparison GET, concurrency 8        |          30 |     64.432 ms |         1,000 ms | Pass   |
| Warm scenario GET, concurrency 8                 |          30 |    333.020 ms |         1,000 ms | Pass   |
| Import recovery after worker SIGKILL             |          10 | 23,589.761 ms |        30,000 ms | Pass   |
| Five-load scenario recovery after worker SIGKILL |          10 | 27,277.055 ms |        30,000 ms | Pass   |

The release qualification used the pinned local release topology with PostgreSQL 16.15 and the S3-compatible store.
It recorded a 208,485,399-byte PostgreSQL database and 18,075,648 bytes across 89 object-store files after the frozen workload.
All 20 worker termination cases recovered with exactly one successful result, two attempts in `EXPIRED` then `SUCCEEDED` order, and an observed worker restart.
No duplicate successful result was observed.

## Claim boundary and deferred gate

The evidence level is `LOCAL_REPRODUCIBLE` and the input scope is public simulated data, locked public fixtures, independent golden inputs, and synthetic engineering workloads.
No private customer data was used.
The evidence does not establish hosted validation, customer-workload performance, multi-host scaling, production ACME operation, or genuine user comprehension.

Run the genuine study gate only after five qualifying sessions have been recorded:

```console
make qualification-m6-study
```

After it passes, run the complete Milestone 8 acceptance check:

```console
make qualification-m8
```

Until both commands pass, Milestone 8 remains `IMPLEMENTED_PENDING_GATE` and must not be described as accepted.
